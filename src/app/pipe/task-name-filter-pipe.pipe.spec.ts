import { TaskNameFilterPipePipe } from './task-name-filter-pipe.pipe';

describe('TaskNameFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TaskNameFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
